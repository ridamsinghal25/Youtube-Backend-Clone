import { asyncHandler } from "../utils/asyncHandler.js";

const uploadVideo = asyncHandler((req, res) => {
  // steps to upload video
  // take video from system
  // validate it - not empty
  // uplooad to cloudinary
});
